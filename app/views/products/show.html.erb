<div class = "container">

  <h1>Products#show</h1>
  <p>Find me in app/views/products/show.html.erb</p>
  <h1> <%= @product.name %></h1>

  <%= link_to "Edit Product Name", edit_product_path(@product), class: 'btn btn-success' %>
  

 <div class="js-uses">  
      <%= render partial: 'uses/use', collection: @uses, locals: {  product: @product } %>
  </div>

  <%= render partial: 'uses/form', locals: { product: @product, use: @use } %>


</div> <!-- container end  -->