
 
 <div class = "container">
  <h1>Add a Product</h1>
   <div class="row">
     <div class="col-md-4">
       <p>Guidelines for products</p>
       <ul>
         <li>Search first and make sure it doesn't already exist</li>
         <li>Only add products that you've actually used</li>
         <li>Use proper product names to the best of your ability</li>
       </ul>
     </div>
     <div class="col-md-8">
       <%= form_for @product do |f| %>
         <div class="form-group">
           <%= f.label :name %>
           <%= f.text_field :name, class: 'form-control', placeholder: "Enter product name" %>
         </div>
         <div class="form-group">
           <%= f.submit "Add Product", class: 'btn btn-success' %>
         </div>
       <% end %>
     </div>
   </div>
  </div>