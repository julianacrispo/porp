<h4> Add a Use Case _form partial </h4>
<%= form_for [product, use], remote: true do |f| %>
<%= f.text_area :description, rows: 4, cols: 40, class: 'form-control', placeholder: "Enter comment" %>
    <%= f.submit "Add Use", class: 'btn btn-success' %>
  </div>
<% end %>